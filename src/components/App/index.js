// ? Import npm
import React from 'react';

// ? Import
import Header from '../Header';
import Court from '../Court';

// ? Import styles
import './styles.scss';

// ? Composant
const App = () => (
  <div className="app">
    <Header />
    <Court />
  </div>
);

// ? Export
export default App;
