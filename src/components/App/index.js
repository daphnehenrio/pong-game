// ? Import npm
import React from 'react';

// ? Import
import Court from '../Court';

// ? Import styles
import './styles.scss';

// ? Composant
const App = () => (
  <div className="app">
    <h1>Pong Game</h1>
    <Court />
  </div>
);

// ? Export
export default App;
